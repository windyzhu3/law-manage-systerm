# Todo 配置中心真实后端设计 QA

> 状态：`PENDING_BROWSER_CHOICE`
> 检查日期：2026-07-21
> 范围：Todo 模板、触发规则、SLA、DoD、模拟与发布记录的真实后端浏览器旅程。

## 1. 当前边界

- 已完成真实 MySQL/Flyway、隔离账号、后端/前端 HTTP、权限与配置目录 API 验证。
- 已完成 Playwright 真实后端用例源码和 CI 门禁接线；核心接口没有 `page.route` 或 `route.fulfill` Mock。
- 尚未选择允许使用的浏览器，因此没有启动 Playwright、没有截取实现截图，也没有作出视觉通过结论。
- 本文不把旧 Mock E2E、源码契约、HTTP 探测或生产构建等同于真实浏览器设计 QA。
- 本轮未通过 HTTP 创建或发布模板；真实数据写入、预检、模拟、发布和清理留给浏览器旅程一次性验证。

## 2. 七个目标状态

| # | 目标状态 | 参考依据 | 参考截图 | 实现截图 | 功能状态 | 视觉评级 | 待检查重点 |
|---:|---|---|---|---|---|---|---|
| 1 | 测试管理员登录并进入 Todo 配置中心 | 仓库现有登录页、侧栏与 Element UI 设计体系 | 待填 | 待填 | HTTP 登录与 6 个动态路由已验证 | `PENDING` | 中文菜单、侧栏层级、选中态、首屏错误 |
| 2 | 模板列表及配置总览 | 现有 Todo 配置列表页 | 待填 | 待填 | 模板列表与 dashboard API 已验证 | `PENDING` | 查询区、表格密度、状态标签、空态/加载态 |
| 3 | 新建模板抽屉初始态 | 现有表单、抽屉和步骤条组件 | 待填 | 待填 | 创建权限已授予；未执行写入 | `PENDING` | 抽屉宽度、必填提示、步骤顺序、按钮层级 |
| 4 | 事件、Owner、SLA、DoD 配置完成态 | 仓库现有规则构建器与字典目录 | 待填 | 待填 | 事件 36、Owner 31、SLA 1、DoD 1 条目录已由真实 API 返回 | `PENDING` | 下拉可读性、长文本、校验反馈、规则摘要 |
| 5 | 草稿保存与发布预检结果 | 现有预检面板和阻断提示 | 待填 | 待填 | 源码旅程已接线；未执行真实写入/预检 | `PENDING` | 成功/阻断层级、可修复项定位、滚动与聚焦 |
| 6 | 真实业务对象模拟结果 | 模拟页、业务对象选择器和结果摘要 | 待填 | 待填 | LEAD 业务对象目录真实 API 返回 1 条；未执行模拟 POST | `PENDING` | 对象检索、请求反馈、结果可扫描性、错误恢复 |
| 7 | 发布确认与发布记录详情 | 发布确认、版本差异和发布记录页 | 待填 | 待填 | 发布记录 API 返回 30 条；未执行新模板发布 | `PENDING` | 高风险确认、不可逆提示、版本号、详情与差异展示 |

## 3. 视觉问题分级

浏览器执行后按以下级别登记；当前没有可评分截图，因此 P0～P3 均为待评，不代表零问题。

| 级别 | 定义 | 当前结论 | 问题与证据 |
|---|---|---|---|
| P0 | 阻断核心旅程、数据错误或无法恢复 | `PENDING` | 待真实浏览器检查 |
| P1 | 关键交互不可用、严重布局/权限/反馈错误 | `PENDING` | 待真实浏览器检查 |
| P2 | 明显视觉偏差或可用性问题，但有绕行路径 | `PENDING` | 待真实浏览器检查 |
| P3 | 文案、间距、对齐、颜色等细节问题 | `PENDING` | 待真实浏览器检查 |

## 4. 执行要求

1. 仅使用用户明确选择的 Chrome 或 Edge；不得自动换用其他浏览器。
2. 使用 1440×900 固定视口，按上表七个状态逐一截图。
3. 同一状态必须同时保存参考截图与实现截图，再进行并排比较；单张截图不能作为通过证据。
4. 对 P0/P1 问题先修复并复测，再处理 P2/P3。
5. Playwright 必须连接真实后端和隔离 MySQL；禁止 Mock 核心 Todo 配置 API。
6. 旅程结束后删除 E2E 模板、版本、关系、模拟与动作记录，并保留清理查询证据。

## 5. 完成条件

只有以下条件全部满足，状态才能从 `PENDING_BROWSER_CHOICE` 改为 `PASSED`：

- 用户已选择浏览器，真实后端 Playwright 用例通过；
- 七个目标状态的参考/实现截图齐全并完成同视口比较；
- P0、P1 为 0，P2/P3 已记录处置结果；
- 新建、保存、预检、模拟、发布、发布详情与数据库清理均有真实证据；
- 没有把浏览器 QA 结果扩大解释为 Foundation 业务准入或 Q/G 审批通过。
