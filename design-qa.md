# Todo 配置中心真实后端设计 QA

> 状态：`PASSED`
> 检查日期：2026-07-21
> 浏览器：Google Chrome（Playwright `channel=chrome`）
> 视口：1672 × 941（与 7 张参考图完全一致）
> 数据环境：隔离 MySQL `law_todo_visual_e2e`，真实 Spring Boot 后端 `127.0.0.1:18081`

## 1. 验证结论

- 六个配置菜单均能从动态菜单进入：待办模板、触发规则、SLA 规则、完成条件、模拟测试、发布记录。
- 八步模板抽屉完整跑通：基础信息、触发条件、负责人、SLA、完成条件、路由、模拟、版本。
- 真实执行了模板创建、两次草稿保存、发布预检、八段模拟结果、正式发布、发布详情查看和测试数据清理。
- 核心配置接口未使用 `page.route` 或 `route.fulfill` Mock。
- SLA 与 DoD 抽屉改用配置中心只读目录接口，修复了 `todo:calendar:manage` 和 `todo:definition:view` 权限误依赖导致的 403。
- 发布后的模板详情优先显示编译快照中的 SLA/DoD 规则名称，不再误报“未绑定规则库”。
- Playwright 用例结束后，模板、版本、模拟记录、动作记录、SLA/DoD 测试规则、测试线索、角色和用户均按运行标记清理。

## 2. 七组参考图对比

实现截图位于 `ruoyi-ui/output/playwright/visual-20260721-31/`（测试证据目录，不提交构建产物）。

| # | 状态 | 实现截图 | 功能结论 | 视觉结论 |
|---:|---|---|---|---|
| 1 | 模板中心与详情 | `01-template-center.png` | 通过 | 使用现有 RuoYi 深色侧栏及 Element 抽屉；信息层级、指标、筛选、表格和详情结构完整 |
| 2 | 八步模板编辑器 | `02-template-editor.png` | 通过 | 按需求采用右侧八步抽屉；组件、间距、按钮与现有系统一致 |
| 3 | SLA 规则 | `03-sla-rules.png` | 通过 | 80/100/150 时间轴、状态、操作和工作日历字段完整；无权限报错 |
| 4 | 完成条件 | `04-dod-rules.png` | 通过 | 字段、材料、条件、校验器和错误文案均可配置；无权限报错 |
| 5 | 触发规则 | `05-trigger-rules.png` | 通过 | 事件版本、条件、目标模板、排序、启停和模拟能力完整 |
| 6 | 模拟结果 | `06-simulation.png` | 通过 | 真实返回状态、模板、负责人、SLA、DoD、路由、卡片和技术日志八段结果 |
| 7 | 发布记录 | `07-release-records.png` | 通过 | 不可变版本、规则快照、语义差异、复制新版本及回滚草稿入口完整 |

参考图为浅色定制后台；实现遵循当前仓库既有 RuoYi/Element UI 设计系统，因此保留深色侧栏、页签栏和模态抽屉。业务布局、操作路径和信息结构以参考图为基准完成。

## 3. 问题分级

| 级别 | 结论 | 处理结果 |
|---|---|---|
| P0 | 0 | 无阻断核心旅程、数据错误或不可恢复问题 |
| P1 | 0 | 已修复目录接口权限错误、抽屉截在动画中、已发布规则名称误报和真实预检/模拟问题 |
| P2 | 0（阻塞项） | 与参考图的视觉差异来自沿用现有系统设计语言，不影响功能与可用性 |
| P3 | 已记录 | 生产构建仍有既有静态资源体积警告，未在本次配置中心范围内扩展处理 |

## 4. 自动化证据

- Chrome 真实后端 E2E：截图模式和无截图 CI 模式均为 `1 passed`，覆盖完整创建—保存—预检—模拟—发布—详情—清理旅程。
- 前端配置中心契约：`npm run test:todo-config` 通过。
- 前端生产构建：`npm run build:prod` 通过，仅保留既有资源体积警告。
- Maven 全量测试：完整 reactor `BUILD SUCCESS`；配置中心新增聚焦测试 57 项通过。
- 真实 MySQL 数据库门禁：10 份必需 Surefire 报告均无跳过、失败或错误；包含草稿保存与预检快照并发冲突回归。
- 视觉截图使用 `animations: 'disabled'`，避免抽屉进入动画造成截断；截图前统一回到页面顶部。

## 5. 边界说明

本报告只证明 Todo Engine 配置中心在隔离真实后端环境中的功能与设计 QA 通过，不等同于 v0.2 全业务模块、Foundation 准入或 Q/G 审批已完成。
