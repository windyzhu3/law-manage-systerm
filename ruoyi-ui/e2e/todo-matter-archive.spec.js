const { test, expect } = require('@playwright/test');const { setupTodo } = require('./fixtures')
test('archive completion exposes decision archive number and material checklist', async ({ page }) => { const capture={};await setupTodo(page,{todo_id:301,todo_no:'TD301',template_code:'CASE_ARCHIVE_CONFIRM',title:'归档确认',business_no:'CS-002',status:'SUBMITTED',priority:'NORMAL',sla_status:'NORMAL'},capture);await page.getByRole('button',{name:'完成'}).click();await expect(page.getByText('确认动作')).toBeVisible();await expect(page.getByText('归档编号')).toBeVisible();await expect(page.getByText('材料清单')).toBeVisible();await expect(page.getByText('+ 添加材料')).toBeVisible() })
